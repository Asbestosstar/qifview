Third-party source is not copied into this archive.

By default CMake fetches pinned versions of SDL3, pugixml, and mapbox/earcut.hpp.
For offline/package-managed builds use QIFVIEWER_FETCH_DEPS=OFF and provide the dependencies locally.
